pipeline {
      agent { label 'built-in' }
      Name=Sandeep
         stages { options {
         stage ('build') {
            steps {
               sh ''' free -h
                     echo "$Name" '''
               }
              }
             }
            } 
               
