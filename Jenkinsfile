pipeline {
      agent { label 'built-in' }
       stages { 
         stage ('build') {
            steps {
               sh '''free -h'''
               }
              }
             }
            } 
               
