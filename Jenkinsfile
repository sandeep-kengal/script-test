pipeline {
      agent { label 'built-in' }
      environment {
      Name="Sandeep"
      }
         stages { 
         stage ('build') {
            steps {
               sh ''' free -h
                     echo "$Name" ''' }
               }
             }
            } 
               
