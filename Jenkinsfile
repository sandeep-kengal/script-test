pipeline {
      agent { label 'build-in' }
       stages { 
         stage ('build') {
            steps {
               sh '''free -h'''
               }
              }
             }
            } 
               
